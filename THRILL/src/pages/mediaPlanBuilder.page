<apex:page >
  <apex:stylesheet value="{!URLFOR($Resource.newApp, 'styles/lib.css')}"></apex:stylesheet>
  <apex:stylesheet value="{!URLFOR($Resource.newApp, 'styles/main.css')}"></apex:stylesheet>
  <head>
    <meta charset="utf-8"></meta>
    <meta name="viewport" content="initial-scale=1, maximum-scale=1, user-scalable=no, width=device-width"></meta>
    <title>newApp</title>
  </head>
  <body>
    <div id="sl" ng-app="demoApp">
      <div id="sl-body">
        <div class="container" ng-view="ng-view"></div>
      </div>
    </div>
    <apex:includescript value="{!URLFOR($Resource.newApp, 'scripts/lib.js')}"></apex:includescript>
    <apex:includescript value="{!URLFOR($Resource.newApp, 'scripts/templates.js')}"></apex:includescript>
    <apex:includescript value="{!URLFOR($Resource.newApp, 'scripts/main.js')}"></apex:includescript>
    <c:ngforce ></c:ngforce>
  </body>

</apex:page>