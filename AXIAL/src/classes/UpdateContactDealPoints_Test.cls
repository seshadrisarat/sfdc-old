@isTest
private class UpdateContactDealPoints_Test {

    static testMethod void myUnitTest() {
        // Just a smoke test
        Test.startTest();
        	Boolean t = true;
	  //      UpdateContactDealPoints obj = new UpdateContactDealPoints();
			//obj.query = 'Select Three_Month_Deal_Points__c, Six_Month_Deal_Points__c, Twelve_Month_Deal_Points__c FROM Contact Limit 100';
			//Database.executeBatch(obj, 100);
        Test.stopTest();
    }
}