<apex:page controller="SL_VCard" cache="true" standardStylesheets="false" showHeader="false" sidebar="false" contenttype="text/x-vcard;#{!vcardName}.vcf">BEGIN:VCARD
<c:SL_VCard firstName="{!firstName}" 
lastName="{!lastName}" 
title="{!title}"
workPhone="{!workPhone}"
mobilePhone="{!mobilePhone}"
email="{!email}"
workCity="{!workCity}"
workStateProvince="{!workStateProvince}"
workStreet="{!workStreet}"
workPostalCode="{!workPostalCode}"
workCountry="{!workCountry}"
photo="{!strPhoto}"
department="{!department}"
company="{!company}"
myOffices="{!myOffices}"> 
</c:SL_VCard>
END:VCARD
</apex:page>