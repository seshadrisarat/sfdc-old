<apex:page standardStyleSheets="false" showHeader="false" sidebar="false" controller="OLE_SiteTemplate">
    
    <head>
        <!-- INCLUDE ALL SCRIPTS & STYLES-->
        <apex:outputPanel rendered="{!IF(strSiteName == 'Ole_Majorly_Indie','TRUE','FALSE')}">
            <c:OLE_IndieJS />
        </apex:outputPanel>
        
        <!--<apex:outputPanel rendered="{!IF(strSiteName == 'Ole_Majorly_Indie','TRUE','FALSE')}">
            <c:JinglePunksJS />
        </apex:outputPanel>-->
        
        <!-- INCLUDE ALL SCRIPTS & STYLES-->
        
        <!-- HEADER COMPONENT -->
        <apex:insert name="header" >
            <apex:outputpanel rendered="{!IF(AND($CurrentPage.name !='Signin',$CurrentPage.name !='Register',$CurrentPage.name !='ForgotYourPassword', strSiteName == 'Ole_Majorly_Indie'),true,false)}">
                <c:OLE_Header />
            </apex:outputpanel>
            
            <!--<apex:outputpanel rendered="{!IF(AND($CurrentPage.name !='Signin',$CurrentPage.name !='Register',$CurrentPage.name !='ForgotYourPassword', strSiteName == 'Ole_Majorly_Indie'),true,false)}">
                <c:OLE_Header />
            </apex:outputpanel>-->
            
            <apex:outputpanel rendered="{!IF(AND(OR($CurrentPage.name =='Signin',$CurrentPage.name =='Register',$CurrentPage.name =='ForgotYourPassword'),strSiteName == 'Ole_Majorly_Indie'),true,false)}">
                <c:OLE_GuestUserHeader />
            </apex:outputpanel>
            
            <!--<apex:outputpanel rendered="{!IF(AND(OR($CurrentPage.name =='Signin',$CurrentPage.name =='Register',$CurrentPage.name =='ForgotYourPassword'),strSiteName == 'Ole_Majorly_Indie'),true,false)}">
                <c:JP_GuestUserHeader />
            </apex:outputpanel>-->
        </apex:insert>
        <!-- HEADER COMPONENT -->
        
    </head>
    
    <body >
        <!-- BODY -->   
        <apex:insert name="body">
        
        </apex:insert>
        <!-- BODY --> 
        
        <!-- FOOTER COMPONENT -->
        <apex:insert name="footer">
            <apex:outputpanel rendered="{!IF($CurrentPage.name !='Signin',true,false)}">
                <c:OLE_Footer />
            </apex:outputpanel>
        </apex:insert>
        <!-- FOOTER COMPONENT -->
        
    </body>
    
</apex:page>